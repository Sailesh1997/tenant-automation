[pytest]
# Default flags applied to every pytest run
addopts =
    --tb=short
    -v
    --alluredir=allure-results

testpaths = tests

# Allure report title shown in browser
# (override per-run via run_tests.py)
